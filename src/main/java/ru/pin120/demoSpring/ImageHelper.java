package ru.pin120.demoSpring;

import org.opencv.core.Mat;
import org.opencv.core.MatOfByte;
import org.opencv.imgcodecs.Imgcodecs;

import java.awt.*;
import java.awt.image.BufferedImage;
import java.io.ByteArrayInputStream;
import java.io.File;

public final class ImageHelper {
//    private Mat mat = new Mat();
//    private static String path = System.getProperty("user.dir")  + File.separator + "photos" + File.separator;
//    public static BufferedImage getImage(byte[] array){
//        MatOfByte matOfByte = new MatOfByte(array);
//        Mat mat = Imgcodecs.imdecode(matOfByte, Imgcodecs.IMREAD_UNCHANGED);
//        BufferedImage image = matToImage(mat);
//        return image;
//    }
//
//    private static BufferedImage matToImage(Mat frame){
//        MatOfByte buffer = new MatOfByte();
//        Imgcodecs.imencode(".png", frame, buffer);
//        return new BufferedImage(new ByteArrayInputStream(buffer.toArray()));
//    }
////    File file = new File(System.getProperty("user.dir")  + File.separator + "photos" + File.separator + doctor.getPathPhoto());
}
